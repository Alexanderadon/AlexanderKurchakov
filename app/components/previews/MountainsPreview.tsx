// Горы в плитке «база» (hero .t-loc). Рисунок — /img/mount.svg, подан маской,
// поэтому цвет берётся из палитры, а сам файл кэшируется браузером.
export function MountainsPreview() {
  return <div className="mnt" aria-hidden="true" />;
}
